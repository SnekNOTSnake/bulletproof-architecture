query Search($first: Int!, $search: String!, $after: String) {
	books(first: $first, search: $search, after: $after) {
		nodes {
			id
			title
			created
			summary
			author {
				id
				name
			}
		}
		pageInfo {
			hasNextPage
			endCursor
		}
	}
}
