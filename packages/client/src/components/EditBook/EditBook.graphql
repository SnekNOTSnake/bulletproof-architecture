mutation UpdateBook(
	$id: ID!
	$title: String!
	$summary: String!
	$content: String!
) {
	updateBook(id: $id, title: $title, summary: $summary, content: $content) {
		id
		title
		lastChanged
		summary
		content
	}
}
